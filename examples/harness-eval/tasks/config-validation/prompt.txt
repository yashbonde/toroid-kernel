Add fail-fast validation for Kernel Config before the kernel allocates stores, connects MCP servers, or builds provider clients. Reject unknown Thinking values, negative MaxRepeatCalls, and context settings where CompactionBufferSize is greater than or equal to TotalContextSize after defaults are applied. Return descriptive errors naming the invalid field and keep zero-value default behavior compatible. Put the validation logic in a focused, directly testable helper and test both invalid and valid boundary cases.
