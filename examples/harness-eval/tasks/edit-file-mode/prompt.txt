Harden the edit tool's write path. A successful edit must preserve the target file's permission bits (including executability) and replace the content atomically so readers cannot observe a partially written file. A failed replacement must leave the original intact, temporary files must not leak, and the existing exact/unique oldText behavior and tool-result contract must remain compatible.
