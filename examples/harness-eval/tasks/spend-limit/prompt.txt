Implement hard USD spend limits in the kernel. Callers need an optional per-call limit for one Kernel.Run or Kernel.Stream invocation and an optional Config-level transcript limit spanning every turn. After each accounted LLM step, whichever applicable limit is reached first must stop the event loop before another LLM request. Preserve existing cost events and cumulative accounting, and cover the behavior with deterministic tests that do not call a real provider.
