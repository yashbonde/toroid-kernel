---
Apply several exact text replacements to one file in a single call.
---

Edits apply in order against the same file. Each oldString must match exactly
and, unless replaceAll is true, appear exactly once. Prefer this over repeated
edit calls on the same file.
