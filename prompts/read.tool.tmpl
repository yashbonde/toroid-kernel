---
Read a file or directory. Images (png/jpg/gif/webp) and PDFs are returned as viewable attachments.
---

filePath should be absolute (relative resolves against the working directory).
Output is line-numbered as "N: text", up to 2000 lines per call — use offset to
continue reading large files (the result says how). Media over 5 MiB is refused.
