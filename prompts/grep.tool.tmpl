---
Search file contents with a regular expression.
---

Searches recursively under path (default: working directory) and returns
matching lines with 2 lines of context. Narrow the path or pattern if the
result is truncated.
